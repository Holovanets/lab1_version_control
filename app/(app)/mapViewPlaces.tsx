import { MapViewScreen } from '@/components'
import { FC } from 'react'
import { Text, View } from 'react-native'

const MapViewPlaces: FC = () => {
  return (
      <MapViewScreen/>
  )
}

export default MapViewPlaces